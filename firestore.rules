rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: Endurecer reglas luego (por dominio, auth, claims, etc.).
      allow read, write: if true;
    }
  }
}
