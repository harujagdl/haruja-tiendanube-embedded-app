rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn()
        && request.auth.token.email in [
          "yair.tenorio.silva@gmail.com",
          "harujagdl@gmail.com",
          "harujagdl.ventas@gmail.com"
        ];
    }

    // ✅ LISTADO PÚBLICO (sin costo/margen/iva)
    match /HarujaPrendas_2025_public/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // 🔒 DATOS SENSIBLES SOLO ADMIN
    match /HarujaPrendas_2025_admin/{docId} {
      allow read, write: if isAdmin();
    }

    // (Opcional) diccionarios públicos
    match /diccionario_{any}/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /counters/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /metadata/{docId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Bloquea todo lo demás por defecto
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
